[Unit]
Description=LiteLLM Proxy Daemon
After=network.target

[Service]
Type=simple
ExecStart=%h/.config/litellm/venv/bin/litellm --config %h/.config/litellm/config.yaml --port 4000
Restart=always
RestartSec=3

[Install]
WantedBy=default.target
